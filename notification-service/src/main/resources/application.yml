server:
  port: 8084
spring:
  application:
    name: notification-service
  kafka:
    # Senior Note: Cấu hình kết nối tới Kafka Broker
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
  cloud:
    config:
      enabled: false
